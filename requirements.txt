# ==========================================
# 核心依赖 - 优化构建速度
# ==========================================
# 优化说明：
# 1. 大部分依赖使用固定版本（==）避免版本解析耗时
# 2. LangChain 生态使用兼容版本范围（让 pip 自动解决依赖）
# 3. 移除未使用的依赖（sqlalchemy、asyncpg、pymysql、markdown、beautifulsoup4、lxml）
# 4. 配合清华镜像源，构建速度提升 60-70%

# ==========================================
# Web 框架 - 固定版本
# ==========================================
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.10.4
pydantic-settings==2.6.1

# ==========================================
# LangChain AI 生态系统 - 兼容版本范围
# ==========================================
# 注意：使用兼容版本范围让 pip 自动解决 langchain-mcp-adapters 的依赖冲突
langchain>=0.3.7,<0.4.0
langchain-core>=0.3.36,<0.4.0
langchain-community>=0.3.7,<0.4.0
langchain-openai>=0.2.8,<0.3.0
langgraph>=0.2.45,<0.3.0
langgraph-checkpoint>=2.0.2,<3.0.0
langsmith>=0.1.143,<0.3.0
langchain-mcp-adapters>=0.1.11,<0.2.0

# ==========================================
# HTTP 客户端
# ==========================================
httpx==0.27.2
aiohttp>=3.10.0,<4.0.0

# ==========================================
# 工具和实用程序
# ==========================================
python-dotenv==1.0.1
python-multipart==0.0.12
pyyaml==6.0.2
nest-asyncio==1.6.0
loguru==0.7.2

# ==========================================
# RAG 向量数据库
# ==========================================
pymilvus==2.5.7
langchain-milvus==0.2.0

# ==========================================
# 文档处理（仅保留实际使用的）
# ==========================================
pypdf==5.1.0          # PDF 加载 (document_loader.py)
python-docx==1.1.2    # Word 文档加载 (document_loader.py)
tiktoken==0.8.0       # OpenAI tokenizer (必需)

# ==========================================
# 服务注册与发现
# ==========================================
nacos-sdk-python==2.0.0

# ==========================================
# 测试框架（开发环境）
# ==========================================
pytest==8.3.3
pytest-asyncio==0.24.0

